---
- name: Creates and deletes s3 bucket
  hosts: localhost 
  tasks:
    - name: S3 management
      aws_s3:
        bucket: "{{bucket}}"
        mode: "{{mode}}"         # get #put #delete #create  #geturl  #getstr  #delobj  #list