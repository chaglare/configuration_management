---
- name: IAM add-role
  hosts: localhost
  tasks:
    - name: IAM add role
      iam_role:
        name: "{{name}}"                                                         #Give a name to the role   Ex:MyNewRole
        state: present
        managed_policy:
          - arn:aws:iam::aws:policy/PowerUserAccess